sudo: required
dist: trusty
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y --no-install-recommends texlive-fonts-recommended texlive-latex-extra texlive-fonts-extra texlive-latex-recommended latex-xcolor pgf
script:
  - pdflatex -interaction=nonstopmode -halt-on-error hydra-architecture-diagram.tex
